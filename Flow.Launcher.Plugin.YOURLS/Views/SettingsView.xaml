<UserControl x:Class="Flow.Launcher.Plugin.YOURLS.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="450" Height="800"
             DataContext="{Binding Path=_settings, RelativeSource={RelativeSource Self}}">
    <Grid Margin="10" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Content="Signature Token" Margin="0,16,0,6"></Label>
        <TextBox Grid.Row="0" Grid.RowSpan="2" Text="{Binding SignatureToken}" Margin="120,11,0,9" HorizontalAlignment="Left" Width="262" />
        
        <Label Grid.Row="1" Content="YOURLS Url" Margin="0,11,0,26" Grid.RowSpan="2"></Label>
        <TextBox Grid.Row="1" Text="{Binding Host}" Margin="0,31,0,0" HorizontalAlignment="Left" Width="400"
                 Grid.RowSpan="2" />

        <Label Grid.Row="2" Content="You need to restart Flow Launcher to apply the changes." FontWeight="Bold"
               Margin="0,45,0,-26">
        </Label>

    </Grid>
</UserControl>